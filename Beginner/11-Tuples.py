# https://www.youtube.com/watch?v=rfscVS0vtbw&t=4737s

coordinates = (4, 5) # Tuples are immutable (cannot be changed once created)
print(coordinates) # Print the tuple
print(coordinates[0]) # Access the tuple

# Tuples are used to store data that should not be changed!